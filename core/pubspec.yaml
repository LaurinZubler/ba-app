name: upsi_core
description: "upsi core package"
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 0.0.1

environment:
  sdk: '>=3.3.4 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  chia_crypto_utils: ^1.0.18
  riverpod_annotation: ^2.3.5
  hooks_riverpod: ^2.5.1
  qr_flutter: ^4.1.0
  mobile_scanner: ^5.1.0
  shared_preferences: ^2.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  freezed: ^2.5.2
  build_runner: ^2.4.9

flutter:
  generate: true